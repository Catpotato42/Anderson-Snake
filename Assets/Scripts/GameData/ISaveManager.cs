

public interface ISaveManager {//I'm following a tutorial for this, so I have no idea if I would normally have to do this or not. This seems exactly like a header file.
    void LoadData (GameData data);
    void SaveData(GameData data);
}
